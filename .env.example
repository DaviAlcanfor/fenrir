# Copy to .env and fill in whatever you have. All free tiers.
GOOGLE_API_KEY=          # https://aistudio.google.com/apikey  (Gemini free tier)
GROQ_API_KEY=            # https://console.groq.com/keys      (free)
OPENROUTER_API_KEY=      # https://openrouter.ai/keys         (:free models)

# Optional
# HEXSTRIKE_SERVER=http://localhost:8888
# HEXSTRIKE_MCP_PATH=/abs/path/to/hexstrike-ai/hexstrike_mcp.py
# REQUIRE_APPROVAL=false   # run offensive tools unattended (don't)

# Model routing is the MODELS table in src/fenrir/config.py — edit it there.
